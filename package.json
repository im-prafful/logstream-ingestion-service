{
  "name": "logstream-ingestion-service",
  "version": "1.0.0",
  "description": "Serverless SQS Polling Pipeline for Log Ingestion to PostgreSQL",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "sam build && sam deploy --guided",
    "deploy": "sam deploy"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.600.0",
    "dotenv": "^16.6.1",
    "pg": "^8.16.3"
  }
}
